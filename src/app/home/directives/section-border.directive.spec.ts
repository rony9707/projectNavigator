import { SectionBorderDirective } from './section-border.directive';

describe('SectionBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionBorderDirective();
    expect(directive).toBeTruthy();
  });
});
